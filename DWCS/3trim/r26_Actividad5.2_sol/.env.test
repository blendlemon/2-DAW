# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/api_libros_test.db"
# DATABASE_URL="sqlite:///:memory:" No funciona con el mismo código, probablemente porque el proceso que crea las tablas y el proceso que ejecuta los tests
# son procesos diferentes, y la base de datos en memoria se pierde al finalizar el proceso que la creó.
APP_ENV=test
DATABASE_URL="mysql://root:abc123.@127.0.0.1:3306/api_libros?serverVersion=8.0.40&charset=utf8mb4"
